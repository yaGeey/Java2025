## `String`

* **Незмінний (immutable)**: після створення змінити його не можна.
* Кожна операція конкатенації (`+`) створює **новий об’єкт**.
* Безпечний для використання, але повільніший при частих змінах.

**Приклад:**

```java
String s = "Hello";
s += " World"; // створюється новий об'єкт, старий не змінюється
```

---

## `StringBuilder`

* **Змінний (mutable)**: змінює свій вміст без створення нових об’єктів.
* **Не синхронізований** → **не потокобезпечний**, але **швидший**.
* Використовується, коли змінюєш рядок багато разів в **одному потоці**.

**Приклад:**

```java
StringBuilder sb = new StringBuilder("Hello");
sb.append(" World"); // змінює існуючий об'єкт
```

---

## `StringBuffer`

* **змінний (mutable)**.
* **Синхронізований** → **потокобезпечний**, але трохи **повільніший** за `StringBuilder`.
* Використовується, якщо кілька потоків змінюють один і той самий рядок.

**Приклад:**

```java
StringBuffer sb = new StringBuffer("Hello");
sb.append(" World");
```

---

## 📌 Порівняння

| Клас            | Змінюваний    | Потокобезпечний | Швидкість                         | Використання                        |
| --------------- | ------------- | --------------- | --------------------------------- | ----------------------------------- |
| `String`        | ❌ (immutable) | ✅               | 🐢 повільний при змінах           | коли рядок не змінюється            |
| `StringBuilder` | ✅             | ❌               | ⚡ дуже швидкий                    | при зміні рядків в одному потоці    |
| `StringBuffer`  | ✅             | ✅               | 🐇 повільніший за `StringBuilder` | при зміні рядків у багатопоточності |

---

📌 **Коротко**:

> * `String` — незмінний
> * `StringBuilder` — змінний і швидкий
> * `StringBuffer` — змінний і потокобезпечний
